# GCP Configuration
# Copy your GCP service account JSON credentials here as a single-line JSON string
# You can get this from Google Cloud Console -> IAM & Admin -> Service Accounts
GCP_CREDENTIALS_JSON={"type":"service_account","project_id":"your-project-id",...}

# GCP Bucket Name
# The name of your GCP Storage bucket where tutorials will be stored
GCP_BUCKET_NAME=learnpack-creator

# Rigo API Configuration
# The Rigo API host URL (defaults to production if not set)
RIGOBOT_HOST=https://rigobot.herokuapp.com

# Pusher Configuration (Server-side)
# These are used in workflows/server-side code
PUSHER_APP_ID=2073209
PUSHER_KEY=609743b48b8ed073d67f
PUSHER_SECRET=ae0ae03cf538441a9679
PUSHER_CLUSTER=us2

# Pusher Configuration (Client-side)
# These are exposed to the browser and used in React components
# Note: NEXT_PUBLIC_ prefix makes these available in the browser
NEXT_PUBLIC_PUSHER_KEY=609743b48b8ed073d67f
NEXT_PUBLIC_PUSHER_CLUSTER=us2
